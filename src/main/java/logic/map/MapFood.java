package logic.map;

public class MapFood extends MapDouble {

    public MapFood(int x, int y, double initValue, String name, double minLimit, double maxLimit) {
        super(x, y, initValue, name, minLimit, maxLimit);
    }
}
