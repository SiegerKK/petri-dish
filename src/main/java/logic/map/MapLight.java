package logic.map;

public class MapLight extends MapDouble {

    public MapLight(int x, int y, double initValue, String name, double minLimit, double maxLimit) {
        super(x, y, initValue, name, minLimit, maxLimit);
    }
}
