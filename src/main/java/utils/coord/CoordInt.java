package utils.coord;

public class CoordInt extends Coord<Integer> {
    /**
     * @param x must be new value, not pointer
     * @param y must be new value, not pointer
     */
    public CoordInt(Integer x, Integer y) {
        super(x, y);
    }
}
