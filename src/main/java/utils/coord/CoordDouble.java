package utils.coord;

public class CoordDouble extends Coord<Double> {
    /**
     * @param x must be new value, not pointer
     * @param y must be new value, not pointer
     */
    public CoordDouble(Double x, Double y) {
        super(x, y);
    }
}
